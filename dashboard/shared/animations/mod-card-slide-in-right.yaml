style: |
  ha-card {
    animation: slideInRight 0.5s cubic-bezier(0.4, 0, 0.2, 1) 0.0s both;
  }

  @keyframes slideInRight {
    from {
      opacity: 1;
      transform: translateX(8px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
